apiVersion: v1
kind: PersistentVolume
metadata:
  name: ca-server-db
  namespace: vbc-net
  labels:
    type: local
spec:
  storageClassName: vbcsc
  capacity:
    storage: 0.5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"